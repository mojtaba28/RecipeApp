<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RecipeApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/darkGreen</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorOnSecondary">@color/dark_gunmetal</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_gunmetal</item>
        <item name="recipeAppTextColor">@color/darker</item>

        <!-- icons for recipes items-->
        <item name="recipeLikeColor">@color/cyanBlueAzure</item>
        <item name="recipeTimeColor">@color/gray</item>
        <item name="recipeVeganColor">@color/gray</item>
        <item name="recipeHealthColor">@color/gray</item>

        <!-- Customize your theme here. -->


    </style>
</resources>